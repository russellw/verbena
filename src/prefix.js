#!/usr/bin/env node
'use strict';

const assert = require('assert');

const abs=Math.abs;
const min=Math.min;
const max=Math.max;
const sqrt=Math.sqrt;

function _prin(a){
	process.stdout.write(a.toString())
}
